"""Infrastructure layer: adapters implementing the application Protocols.

This layer owns all I/O (filesystem, JSON, CSV, the wall clock). It is never
imported by the domain or the application layer.
"""
