-r requirements.txt
pytest>=8.0
pytest-cov>=5.0
pytest-qt>=4.4
Pillow>=10.0
black>=24.0
flake8>=7.0
# Nuitka compiles the packaged Windows and macOS builds (buildexe.py,
# buildinstaller.py, builddmg.py). The Linux Flatpak pulls Nuitka into its own
# build sandbox, so it does not depend on this entry.
Nuitka>=2.0
