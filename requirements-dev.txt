-r requirements.txt
pytest>=8.0
pytest-cov>=5.0
pytest-qt>=4.4
Pillow>=10.0
black>=24.0
flake8>=7.0
ruff>=0.4
# Nuitka compiles the packaged Windows and macOS builds (buildexe.py,
# buildinstaller.py, builddmg.py). The Linux Flatpak runs from source.
Nuitka>=2.0
